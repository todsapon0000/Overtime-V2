const otRecordSchema = {
    // --- ข้อมูลวันที่ & การกรอง ---
    date: "2026-07-22",                  // วันที่ปฏิบัติงาน (YYYY-MM-DD) ใช้แสดงตาราง เรียงลำดับ และออก PDF
    month_round: 7,                     // รอบเดือนรับเงิน (เช่น 7 คือรอบตัดเงิน 21 มิ.ย. - 20 ก.ค.) ใช้กรอง Query

    // --- เวลาปฏิบัติงาน ---
    time_in: "08:30",                   // เวลาเข้างาน (ระบบ 24 ชม. HH:mm)
    time_out: "21:30",                  // เวลาเลิกงาน (ระบบ 24 ชม. HH:mm)

    // --- เงื่อนไขการคำนวณ (Flags) ---
    is_holiday: false,                  // เช็กวันหยุดนักขัตฤกษ์ (true = วันหยุด, false = วันธรรมดา)
    is_ot_break: false,                 // เช็กพักเบรกก่อน OT (true = หักเวลาพักเบรก, false = ไม่หัก/ทำยิงยาว)

    // --- สถานที่ทำงาน (สำหรับออก PDF) ---
    work_place_type: "outside",         // ประเภทสถานที่ ("inside" = งานใน, "outside" = งานนอก) ใช้ติ๊กถูก ✓ ใน PDF
    work_place_name: "Site Chonburi",   // ชื่อสถานที่ปฏิบัติงาน สำหรับพิมพ์ลงในช่อง Working Place

    // --- ผลลัพธ์การคำนวณชั่วโมงและยอดเงิน (Snapshot Data) ---
    ot_1_5_hours: 4.0,                  // จำนวนชั่วโมง OT เรท 1.5
    ot_1_5_amount: 400,                 // ยอดเงินรวม OT เรท 1.5
    ot_3_0_hours: 0.0,                  // จำนวนชั่วโมง OT เรท 3.0
    ot_3_0_amount: 0,                   // ยอดเงินรวม OT เรท 3.0
    meal_count: 2,                      // จำนวนมื้อ/ค่าอาหาร (นำไปคูณ 50 บาท หรือนับมื้อลง PDF)
    total_hours: 4.0,                   // จำนวนชั่วโมง OT รวมของวันนั้น
    total_amount: 500,                  // ยอดเงินรวมทั้งหมดของวันนั้น (ค่า OT + ค่าอาหาร)

    // --- หมายเหตุ & ระบบ ---
    remark: "ตรวจหน้างาน",             // หมายเหตุเพิ่มเติม (ระบุรายละเอียดงาน)
    created_at: firebase.firestore.FieldValue.serverTimestamp(), // เวลาที่บันทึกข้อมูลเข้า Firebase (Timestamp)
    updated_at: firebase.firestore.FieldValue.serverTimestamp()  // เวลาที่แก้ไขข้อมูลล่าสุด (Timestamp)
};